import { describe, it, expect } from 'vitest';

describe('Request hash', () => {
	it.skip('produces stable hash for identical inputs', () => {
		expect(true).toBe(false);
	});
	it.skip('produces different hash for different inputs', () => {
		expect(true).toBe(false);
	});
	it.skip('ignores key ordering in objects', () => {
		expect(true).toBe(false);
	});
});
