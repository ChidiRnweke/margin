import { describe, it, expect } from 'vitest';

describe('Cursor codec', () => {
	it.skip('encodes and decodes cursor round-trip', () => {
		expect(true).toBe(false);
	});
	it.skip('rejects invalid cursor string', () => {
		expect(true).toBe(false);
	});
	it.skip('rejects tampered cursor', () => {
		expect(true).toBe(false);
	});
	it.skip('validates cursor shape against query type', () => {
		expect(true).toBe(false);
	});
});
