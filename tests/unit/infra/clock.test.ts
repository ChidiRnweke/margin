import { describe, it, expect } from 'vitest';

describe('Clock', () => {
	it.skip('returns current UTC time', () => {
		expect(true).toBe(false);
	});
	it.skip('injectable clock allows test time control', () => {
		expect(true).toBe(false);
	});
});

describe('Timezone handling', () => {
	it.skip('handles DST transition correctly', () => {
		expect(true).toBe(false);
	});
	it.skip('resolves IANA timezone to UTC offset', () => {
		expect(true).toBe(false);
	});
});
